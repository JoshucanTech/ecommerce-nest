generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:secret2580@localhost:5432/ecommerce?schema=public"
}
